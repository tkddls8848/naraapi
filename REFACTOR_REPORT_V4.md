# NARA-P 리팩터링 결과 보고서 v4.0

## 1. 범위와 원칙

이번 작업은 데이터 계층, 나라장터 API 계층, 애플리케이션 내부 변경 요청, 전체 화면을 한 번에 정리한 통합 리팩터링이다. 목표는 오래된 Express/Route Handler 형태를 흉내 내던 구조와 위치 기반 데이터를 없애고, Next.js 16 App Router와 React 19의 현재 사용 방식에 맞추는 것이었다.

인증의 보안 수준은 의도적으로 바꾸지 않았다. 비밀번호 평문 비교, JWT payload decode, 쿠키 존재 여부 중심의 가드는 보안상 개선 대상이지만 이번 범위에서 바꾸면 기존 로그인 의미와 배포 동작이 달라지기 때문이다. 중복된 인증 코드는 `frontend/lib/auth.js`로 모았지만 판정 기준 자체는 유지했다.

## 2. 무엇을 왜 바꿨는가

### 데이터 계층

- Drizzle 프로퍼티는 camelCase, PostgreSQL 컬럼은 snake_case로 명시적으로 매핑했다.
- 잘못된 `e_mail` 컬럼을 `email`로 정리했다. Mongo 이관기만 과거 입력 호환을 위해 `e_mail`을 fallback으로 읽는다.
- 사용 코드가 없던 `archives` 테이블을 Drizzle 선언과 초기 DDL에서 제거했다.
- `user_tasks`에 `notice_id`, `notice_url`을 추가했다. 제목만 저장해 원 공고를 다시 열 수 없던 문제를 해결하기 위한 변경이다.
- `(user_id, task_type, notice_id)` 유니크 제약으로 한 사용자의 동일 공고 중복 저장을 막았다.
- 사용자 삭제 시 저장 공고가 함께 삭제되도록 FK의 `ON DELETE CASCADE`를 유지하고, 사용자 조회 인덱스를 DDL과 Drizzle 양쪽에 맞췄다.

### 나라장터 데이터 계층

- 서로 다른 원본 응답을 `{ id, title, departName, registeredAt, closesAt, fileUrl, kind, isNew, raw }` 객체로 정규화했다.
- 사전규격/본공고의 물품·공사·용역 요청을 공통 모듈로 합치고, 일부 요청 실패가 전체 결과를 없애지 않도록 `Promise.allSettled`로 수집한다.
- 기관 검색은 `/list?query=...`의 실제 검색어를 사용하고, 동적이지만 쓰이지 않던 `/list/[pageNum]` 경로를 `/list`로 단순화했다.
- 기관별 기간 검색과 다기관 오늘 공고 조회가 같은 정규화 계약을 사용하도록 맞췄다.

### Server Actions와 인증 가드

- `frontend/app/api` 전체를 제거했다. 앱 내부 폼이 자신의 Route Handler를 다시 `fetch`하던 왕복 대신 `auth-actions.js`와 `user-task-actions.js`를 직접 사용한다.
- 로그인, 가입, 정보 수정, 로그아웃, 탈퇴, 공고 저장/삭제를 Server Action으로 옮겼고 변경 뒤 필요한 경로를 `revalidatePath`로 갱신한다.
- `STATUS_*` 환경 변수 7개를 제거했다. 프로토콜 결과는 환경 설정이 아니라 `{ ok, reason }` 반환 객체로 표현한다.
- 여덟 페이지에 반복되던 쿠키 읽기, JWT decode, redirect를 `getCurrentUser`와 `requireUser`로 모았다.
- 저장 공고 조회는 필드 순서를 암묵적으로 알아야 하는 튜플 대신 이름 있는 객체 배열을 반환한다.
- 삭제 입력의 이름을 실제 의미인 `contentNumber`로 통일했다. 과거에는 `userId`처럼 보이는 경로 자리에 사실상 공고 번호가 전달됐다.

### 화면

- 인증/회원 폼과 공고 저장·삭제 폼을 `useActionState`와 `useFormStatus` 기반으로 바꿨다. 제출 중 버튼을 비활성화하고 성공·실패를 `alert()` 대신 인라인 상태로 표시한다.
- 가입/수정 화면은 사용자가 비밀번호 확인을 입력하기 전에는 불일치 오류를 보여 주지 않는다.
- 단순 이동은 `button + router.push` 대신 `Link`를 사용하고, 404 화면도 클라이언트 라우터 없이 렌더링한다.
- `upgrade-insecure-requests` CSP를 문서 meta 태그에서 Next.js `headers()` 설정으로 이동했다.
- 공고 저장 버튼은 정규화된 공고 ID와 링크를 전달하고, 저장 목록은 해당 링크를 다시 열 수 있다.

## 3. 실제로 고친 버그

- 저장 목록에 제목만 남아 원 공고를 다시 열 수 없던 문제
- 같은 사용자가 같은 공고를 여러 번 저장할 수 있던 문제
- 저장 공고 삭제 인자의 이름과 실제 값이 달라 계층 간 의미가 어긋난 문제
- 본공고 수집에서 물품 공고가 빠지고 일부 분류가 중복되던 문제
- 공공 API 일부 요청 실패가 성공한 다른 분류 결과까지 모두 버리던 문제
- 기관 검색이 사용자의 검색어를 무시하던 문제와 사용하지 않는 페이지 번호 경로
- 사전규격이 0건이면 본공고가 있어도 전체를 데이터 없음으로 표시하던 문제
- 오늘 공고 기관 선택에서 유형이 사실상 본공고로 고정되던 문제
- 빈 입력 상태부터 비밀번호 불일치 안내가 보이던 문제
- 가입·수정·로그인·로그아웃·탈퇴 피드백이 브라우저 `alert()`에만 의존하던 문제

## 4. 판단이 갈렸던 지점

### 인증을 더 안전하게 만들지 않은 이유

중복 제거와 보안 강화는 별개의 변경으로 판단했다. 서명 검증, 비밀번호 해시, 쿠키 속성 강화는 필요하지만 기존 쿠키를 읽는 클라이언트와 로그인 판정 기준을 동시에 바꾼다. 이번 버전은 공통 헬퍼로만 모으고 동작은 유지했으며, 보안 마이그레이션은 별도 작업으로 남겼다.

### `notice_id`와 `notice_url`을 nullable로 둔 이유

기존 Mongo 문서에는 두 값이 없다. DDL에서 즉시 NOT NULL로 만들면 과거 데이터를 손실 없이 가져올 수 없으므로 이관 호환성을 위해 NULL을 허용했다. 대신 신규 Server Action은 빈 `noticeId` 저장을 거부한다. PostgreSQL의 UNIQUE 제약은 NULL끼리 중복으로 보지 않으므로, 이관기는 ID 없는 과거 행을 사용자·유형·제목으로 추가 확인해 재실행 중복을 줄인다.

### 내부 REST API를 유지하지 않은 이유

외부 소비자가 없는 동일 애플리케이션 내부 경로였고, 모든 호출자가 폼 또는 서버 페이지였다. 따라서 Route Handler 계약을 억지로 유지하기보다 Server Action과 서버 모듈로 역할을 나누는 편이 인증 컨텍스트, 타입 모양, 화면 갱신을 한곳에서 관리하기 쉬웠다.

## 5. 검증 결과

`frontend` 디렉터리에서 다음 정적 검증을 완료했다.

- `npx eslint .`: exit 0, 오류 0, 경고 0
- `npx prettier --check .`: exit 0
- `npm run build`: exit 0
- `frontend/app/api` 디렉터리 부재 확인
- 소스의 `STATUS_*` 환경 변수 참조 0건 확인
- 저장 공고 위치 기반 튜플 접근 0건 확인
- 기존 동작을 보존한다는 `FIXME` 주석 0건 확인
- 삭제된 Route Handler 경로를 호출하는 앱 `fetch` 0건 확인
- Drizzle에서 예전 snake_case JavaScript 프로퍼티를 참조하는 코드 0건 확인

프로덕션 빌드는 `/`, 회원 화면, `/task`와 두 공고 결과 경로, `/todaytask`, `/todaytask/show`, `/usertask`, `/list`를 모두 정상적으로 수집했다.

## 6. 검증하지 못한 것

작업 머신에는 실행 가능한 PostgreSQL DB와 `SERVICE_KEY`가 없었다. 따라서 실제 DB 연결, 초기 DDL 적용, 기존 운영 DB의 `ALTER` 마이그레이션, Mongo 데이터 이관 실행, 나라장터 실응답, 로그인부터 공고 저장·삭제까지의 브라우저 E2E는 런타임으로 검증하지 못했다. `next build` 성공은 모듈 배선과 렌더 가능성을 확인하지만 외부 시스템과 데이터 품질까지 보장하지는 않는다.

## 7. 배포 전 확인 사항

1. 운영 DB를 백업한 뒤 `postgres/migrations/v3.2-to-v4.0.sql`을 적용한다. init 스크립트는 데이터가 있는 볼륨에서 실행되지 않으므로, 적용하지 않으면 기동 후 첫 쿼리에서 깨진다.
2. `frontend/config_files/.env.example`을 기준으로 `.env.production`을 채운다.
3. 실제 `SERVICE_KEY`로 기관 검색, 기관별 공고, 다기관 오늘 공고를 확인한다.
4. 신규/기존 사용자 각각으로 로그인, 수정, 로그아웃, 탈퇴와 공고 저장·중복 저장·삭제를 확인한다.
5. 과거 Mongo 데이터를 가져오는 경우 ID 없는 저장 공고의 링크 부재가 의도한 호환 동작인지 확인한다.
